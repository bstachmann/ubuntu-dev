---
- hosts: localhost

  vars_files:
    - ~/.ubuntu-dev-vars.yml

  roles:
    - setup
    - development-tools
    - role: ansible-role-atom-packages
#      become: false
      atom_packages_packages:
        - language-elm
    - writer-tools
    - photo-tools
